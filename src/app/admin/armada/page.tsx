import { AdminConsole } from "@/components/admin/admin-console";

export const metadata = { title: "Armada" };

/** Fleet management — the catalogue behind `/armada` and the booking flow. */
export default function AdminArmadaPage() {
  return <AdminConsole />;
}
